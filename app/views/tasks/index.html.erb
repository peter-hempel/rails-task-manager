<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <%= link_to task.title,  task_path(task) %>

    <% if task.completed? %>
      <p> [x] <%= task.title %></p>
    <% else %>
      <p> [ ] <%= task.title %></p>
    <% end %>
  <% end %>
</ul>


